import React from 'react';
import './notFound.css'

const NotFound = () => {
    return (
        <section className='notFound'>
            notFound
        </section>
    );
};

export default NotFound;